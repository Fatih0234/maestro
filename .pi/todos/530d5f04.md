{
  "id": "530d5f04",
  "title": "Phase 3: Orchestrator — Event System, Backoff, Main Loop",
  "tags": [
    "phase-3",
    "orchestrator",
    "events",
    "backoff",
    "main-loop"
  ],
  "status": "done",
  "created_at": "2026-04-21T16:59:17.360Z"
}

# Phase 3: Orchestrator — Event System, Backoff, Main Loop

> **Status:** ✅ COMPLETED

Implemented all components according to [docs/context/phase-3-orchestrator.md](./docs/context/phase-3-orchestrator.md)

## Completed Components

### 3.1 Event System (`internal/orchestrator/events.go`)
- `OrchestratorEvent` struct and 13 event type constants
- Payload structs for all event types

### 3.2 Backoff Logic (`internal/orchestrator/backoff.go`)
- `BackoffManager` with exponential backoff + ±20% jitter
- `CalculateDelay()`, `Enqueue()`, `Ready()`, `Remove()`, `Get()`, `Len()`

### 3.3 State Management (`internal/orchestrator/state.go`)
- `RunState` struct tracking issue execution
- `StateManager` with `Add()`, `UpdatePhase()`, `UpdateTokens()`, `UpdateLastEvent()`, `SetError()`, `Get()`, `Remove()`, `Len()`, `GetAll()`, `GetByPhase()`

### 3.4 Main Loop (`internal/orchestrator/orchestrator.go`)
- `Orchestrator` struct with all dependencies
- `New()`, `Run()` (main polling loop), `poll()`
- `reconcileRunning()` for stall/timeout detection
- `handleTimeout()`, `handleStall()` for timeout/stall handling
- `dispatchBackoff()` for retrying failed issues
- `dispatchReady()` for claiming new issues
- `startRun()` for starting a run (workspace → prompt → agent)
- `monitorAgent()` goroutine for event stream handling
- `handleAgentDone()` for completion/error handling
- `buildPrompt()` with template variable substitution
- `emit()` helper for non-blocking event emission
- `shutdown()` for graceful shutdown

## Files Created

```
internal/orchestrator/
├── events.go        # OrchestratorEvent type + constants + payload structs
├── backoff.go      # BackoffManager
├── backoff_test.go # Unit tests
├── state.go        # StateManager for active runs
├── state_test.go   # Unit tests
└── orchestrator.go # Main Orchestrator struct + loop
```

## Verification

- ✅ All tests pass
- ✅ Build succeeds
- ✅ Event type constants defined (13 types)
- ✅ Backoff delays follow formula with jitter
- ✅ StateManager operations working correctly
